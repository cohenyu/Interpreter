//
// Created by yuval on 24/12/18.
//

#include <iostream>
#include "EnterC.h"

/**
 * The function waits to receive input from the user.
 * @param data the main vector
 * @param index the index if the command on the vector
 * @return how mach moves to step after this command.
 */
int EnterC::doCommand(vector<string> data, int index) {
    char c;
    cin >> c;
    return 1;
}

//constructor
EnterC::EnterC() {

}
